# 旧世界观提示词模板

`innerworld_lore.md` 已不再是运行时必需文件，正式世界观入口改为运行目录配置中的 `WORLD_FILE`。
本模板仅作为旧配置迁移参考。真实世界观、私人设定、成员关系和群聊约定请写入外部私有路径，例如 `config/world.md`，并通过 `WORLD_FILE` 指定。

## 约束

- 不写入真实用户信息、群聊内容或私人关系。
- 不写入不希望开源公开的角色设定。
